

<h1>TEMPLATE INDEX</h1>


<% data.forEach(function (item){ %>

    <% for (const [key, value] of Object.entries(item["_doc"])) { %>
        <p><%= key %> : <%= value %></p>
    <% } %>

    <br>    

<% }) %>